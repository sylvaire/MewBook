# Milestone Archive — v1.1.0

**Completed:** 2026-05-29
**Status:** Locally complete and archived; remote push/release still needs valid GitHub credentials or SSH host-key access.
**Local tag:** `v1.1.0` points at the latest local HEAD at archive time.

## Scope

v1.1.0 completed the release polish around category management, settings/version details, haptic feedback, and release metadata.

## Completion Evidence

| Area | Evidence |
|------|----------|
| Versioning | `app/build.gradle.kts` uses `versionName = "1.1.0"` and `versionCode = 13`. |
| Category cleanup | `DefaultCategories` no longer exposes retired default secondary categories; `CategorySelectionPolicy.visibleCategories()` drives category management and new-record selection. |
| Category consistency | `CategorySelectionPolicyTest.recordSelectionCandidates_matchCategoryManagementForNewRecords` locks category management and record-entry consistency. |
| Category icons/UI | `CategoryIconOptionsTest` locks key vehicle/transport/home/medical/shopping/finance icon options; `CategoriesScreen` no longer shows color dots or the right edit icon. |
| Settings/version | `SettingsScreen` opens `AppInfoDialog` from the version card; the dialog contains the project GitHub URL and manual `检查更新`. |
| Update entry consolidation | Settings keeps the automatic update toggle; manual checking lives in the version details dialog. |
| Haptics | `HapticFeedbackPolicy` and `MewHapticFeedback` gate amount-keyboard, settings, period, quick-entry, dialog, and external-link feedback; `HapticFeedbackPolicyTest` covers the policy. |
| Release docs | `README.md`, `AGENTS.md`, `.planning/codebase/*`, and `.github/workflows/release.yml` were synchronized for 1.1.0. |

## Verification

- `git diff --check`
- `testDebugUnitTest :app:lintDebug :app:assembleDebug :app:assembleRelease -Dandroid.enableJetifier=false`

Gradle completed with `BUILD SUCCESSFUL`. Logs still contained the known Kotlin daemon `AccessDeniedException` fallback and SDK XML warning, but the build and checks passed.

## Archived Source Documents

- `.planning/PROJECT.md`
- `.planning/REQUIREMENTS.md`
- `.planning/ROADMAP.md`
- `.planning/STATE.md`
- `.planning/phases/04-haptic-feedback/*`
- `.planning/phases/05-category-cleanup-reorder/*`
- `.planning/phases/06-input-settings-polish/*`
- `.planning/codebase/*`

These files remain in place for continuity; this archive is the milestone-level handoff snapshot.

## Remaining Operational Work

The milestone is implementation-complete, but GitHub publication did not finish in the current sandbox:

- `gh auth status` reported an invalid token.
- Sandbox `git push` failed because SSH could not read `known_hosts`.
- Escalated push approval timed out.

Publish from an environment with valid credentials:

```powershell
git push origin HEAD:codex/full-app-rewrite
git push origin v1.1.0
```

Pushing the tag triggers `.github/workflows/release.yml` to build the release APK and publish the GitHub Release.
