---
phase: 05-category-cleanup-reorder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/main/java/com/mewbook/app/domain/model/Category.kt
  - app/src/main/java/com/mewbook/app/domain/policy/CategorySelectionPolicy.kt
autonomous: true
requirements:
  - CAT-01

must_haves:
  truths:
    - "DefaultCategories 不再暴露 legacyExpenseSubCategoryNames、flatExpenseAdditions、recordEntryExpenseCategories 字段"
    - "所有支出分类（原 base + additions 共 87 项）统一出现在单一的 expenseCategories 列表中，sortOrder 连续化"
    - "CategorySelectionPolicy.recordSelectionCandidates 对所有 RecordType 返回全部可见分类，不按 legacy 名称做过滤"
    - "AddEditRecordSheet.kt 和 HomeQuickEntryCategoryPolicy.kt 无需修改即可编译通过"
  artifacts:
    - path: "app/src/main/java/com/mewbook/app/domain/model/Category.kt"
      provides: "统一支出分类定义"
      contains: "expenseCategories"
      not_contains:
        - "flatExpenseAdditions"
        - "legacyExpenseSubCategoryNames"
        - "recordEntryExpenseCategories"
    - path: "app/src/main/java/com/mewbook/app/domain/policy/CategorySelectionPolicy.kt"
      provides: "无过滤的分类选择策略"
      contains: "recordSelectionCandidates"
      not_contains:
        - "RecordEntryExpenseCategoryNames"
        - "LegacyExpenseSubCategoryNames"
  key_links:
    - from: "AddEditRecordSheet.kt"
      to: "CategorySelectionPolicy.recordSelectionCandidates"
      via: "函数调用（line 160）"
      pattern: "CategorySelectionPolicy\\.recordSelectionCandidates"
    - from: "HomeQuickEntryCategoryPolicy.kt"
      to: "CategorySelectionPolicy.recordSelectionCandidates"
      via: "函数调用（line 17）"
      pattern: "CategorySelectionPolicy\\.recordSelectionCandidates"
    - from: "InitializeDefaultCategoriesUseCase"
      to: "DefaultCategories.all"
      via: "val defaultCategories = DefaultCategories.all"
      pattern: "DefaultCategories\\.all"
---

<objective>
删除 Category.kt 中残留的旧二级分类内部数据结构（flatExpenseAdditions、legacyExpenseSubCategoryNames、recordEntryExpenseCategories），将所有支出分类合并为单一统一列表。同步清理 CategorySelectionPolicy.kt 中基于 legacy 名称的过滤逻辑。

Purpose: 消除旧二级分类设计在内部模型中的最后残留，使所有分类在记账选择、统计下钻、首页快捷入口中平等可见。
Output: Category.kt 仅维护单一 baseExpenseCategories + expenseCategories；CategorySelectionPolicy 对收支类型一视同仁。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
<interfaces>
<!-- Key types extracted from codebase. Executor should use these directly. -->

From src/domain/model/Category.kt:
```kotlin
data class Category(
    val id: Long = 0,
    val name: String,
    val icon: String,
    val color: Long,
    val type: RecordType,
    val isDefault: Boolean,
    val sortOrder: Int
)

object DefaultCategories {
    private val baseExpenseCategories = listOf(/* 36 items, sortOrder 0-35 */)
    private val flatExpenseAdditions = listOf(/* 51 items, all sortOrder=0 */)
    val recordEntryExpenseCategories = normalizeSortOrder(baseExpenseCategories)
    val legacyExpenseSubCategoryNames = flatExpenseAdditions.map(Category::name).toSet()
    val expenseCategories = normalizeSortOrder(recordEntryExpenseCategories + flatExpenseAdditions)
    val incomeCategories = listOf(/* 8 items */)
    val all = expenseCategories + incomeCategories
    private fun normalizeSortOrder(categories: List<Category>): List<Category> {
        return categories.distinctBy { it.type to it.name }
            .mapIndexed { index, category -> category.copy(sortOrder = index) }
    }
}
```

From src/domain/policy/CategorySelectionPolicy.kt:
```kotlin
object CategorySelectionPolicy {
    private val RecordEntryExpenseCategoryNames = DefaultCategories.recordEntryExpenseCategories
        .map(Category::name).toSet()
    private val LegacyExpenseSubCategoryNames = DefaultCategories.legacyExpenseSubCategoryNames

    fun visibleCategories(categories: List<Category>, type: RecordType): List<Category>
    // ── 当前 EXPENSE 特殊逻辑 ──
    fun recordSelectionCandidates(...): List<Category> {
        if (type != RecordType.EXPENSE) return visibleCategories(categories, type)
        return categories.filter { it.type == RecordType.EXPENSE }
            .filter { category ->
                category.name in RecordEntryExpenseCategoryNames ||
                    (!category.isDefault && category.name !in LegacyExpenseSubCategoryNames) ||
                    category.id == selectedCategoryId
            }
            .sortedWith(compareBy(Category::sortOrder, Category::name, Category::id))
            .toList()
    }
}
```

From src/domain/usecase/category/CategoryUseCases.kt:
```kotlin
class InitializeDefaultCategoriesUseCase {
    // 使用 DefaultCategories.all 初始化
    // 使用 categoryKey = "${category.type.name}:${category.name}" 去重
}
```
</interfaces>

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-category-cleanup-reorder/05-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge flatExpenseAdditions into baseExpenseCategories and remove legacy fields</name>
  <files>app/src/main/java/com/mewbook/app/domain/model/Category.kt</files>

  <read_first>
Read the following files before making any changes:
1. `app/src/main/java/com/mewbook/app/domain/model/Category.kt` — full file, understand current structure
2. `app/src/main/java/com/mewbook/app/domain/usecase/category/CategoryUseCases.kt` — understand InitializeDefaultCategoriesUseCase to ensure .all compatibility
  </read_first>

  <action>
**Step 1 — 合并列表：**
将 `flatExpenseAdditions`（lines 53-105）中的所有 51 个 Category 条目追加到 `baseExpenseCategories`（lines 14-51）的结束花括号之前。合并后的 `baseExpenseCategories` 包含原 36 条 + 51 条 = 87 条。保留原 `baseExpenseCategories` 中每个条目的 sortOrder 值不变，新增条目保持 sortOrder=0。

**Step 2 — 统一 expenseCategories：**
删除以下三个字段：
- `val recordEntryExpenseCategories = normalizeSortOrder(baseExpenseCategories)` (line 107)
- `val legacyExpenseSubCategoryNames = flatExpenseAdditions.map(Category::name).toSet()` (line 108)
- 整个 `flatExpenseAdditions` 变量及其列表内容 (lines 53-105)

将 `val expenseCategories` (line 109) 改为直接引用合并后的 baseExpenseCategories：
```kotlin
val expenseCategories = normalizeSortOrder(baseExpenseCategories)
```

**Step 3 — 验证 all 字段：**
`val all = expenseCategories + incomeCategories` (line 122) 保持不变，表达式自动引用新的统一 expenseCategories。

**Step 4 — 保留 normalizeSortOrder：**
`private fun normalizeSortOrder` (lines 124-128) 保持不变，它继续对合并后的列表去重并连续化 sortOrder。

**最终状态：** Category.kt 中的 DefaultCategories 仅包含：
- `baseExpenseCategories`（87 条，合并后）
- `expenseCategories`（normalizeSortOrder(baseExpenseCategories)）
- `incomeCategories`（8 条，不变）
- `all`（不变）
- `normalizeSortOrder()`（不变）
  </action>

  <acceptance_criteria>
1. `grep -n "flatExpenseAdditions" app/src/main/java/com/mewbook/app/domain/model/Category.kt` 无任何输出
2. `grep -n "legacyExpenseSubCategoryNames" app/src/main/java/com/mewbook/app/domain/model/Category.kt` 无任何输出
3. `grep -n "recordEntryExpenseCategories" app/src/main/java/com/mewbook/app/domain/model/Category.kt` 无任何输出
4. `grep -n "val expenseCategories" app/src/main/java/com/mewbook/app/domain/model/Category.kt` 仅匹配到 `normalizeSortOrder(baseExpenseCategories)` 一行
5. `grep -n "val all" app/src/main/java/com/mewbook/app/domain/model/Category.kt` 匹配到 `val all = expenseCategories + incomeCategories`
6. `grep -c "Category(name" app/src/main/java/com/mewbook/app/domain/model/Category.kt` 输出 95（87 支出 + 8 收入）
7. 文件可被 Kotlin 编译器解析（无语法错误——由主项目构建验证）
  </acceptance_criteria>

  <verify>
    <automated>cd D:/Codes/AndroidStudioProjects/MewBook && ./gradlew :app:compileDebugKotlin 2>&1 | tail -20</automated>
  </verify>

  <done>
Category.kt 中不再存在 flatExpenseAdditions、legacyExpenseSubCategoryNames、recordEntryExpenseCategories。baseExpenseCategories 包含 87 条合并后的支出分类。expenseCategories 通过 normalizeSortOrder 返回连续 sortOrder 的统一列表。
  </done>
</task>

<task type="auto">
  <name>Task 2: Simplify recordSelectionCandidates to remove legacy subcategory filtering</name>
  <files>app/src/main/java/com/mewbook/app/domain/policy/CategorySelectionPolicy.kt</files>

  <read_first>
Read the following files before making any changes:
1. `app/src/main/java/com/mewbook/app/domain/policy/CategorySelectionPolicy.kt` — full file
2. `app/src/main/java/com/mewbook/app/ui/screens/add/AddEditRecordSheet.kt` — lines 156-168 (understand how recordSelectionCandidates is consumed)
3. `app/src/main/java/com/mewbook/app/domain/policy/HomeQuickEntryCategoryPolicy.kt` — line 17 (second consumer)
  </read_first>

  <action>
**Step 1 — 删除旧分类名称缓存：**
删除以下两个私有属性：
- lines 9-11: `private val RecordEntryExpenseCategoryNames = DefaultCategories.recordEntryExpenseCategories.map(Category::name).toSet()`
- line 12: `private val LegacyExpenseSubCategoryNames = DefaultCategories.legacyExpenseSubCategoryNames`

**Step 2 — 简化 recordSelectionCandidates：**
将 `recordSelectionCandidates` 方法体（lines 25-44）替换为对所有类型一致的简单实现：

```kotlin
fun recordSelectionCandidates(
    categories: List<Category>,
    type: RecordType,
    selectedCategoryId: Long? = null
): List<Category> {
    return visibleCategories(categories, type)
}
```

移除 EXPENSE 类型的特殊分支（原 lines 30-41 的条件分支和 legacy 名称过滤）。

注意：`selectedCategoryId` 参数保留在签名中但不再使用，因为简化后所有分类都可见，无需特殊处理已选分类。保留参数以避免修改所有调用方的签名。

**Step 3 — 清理 imports：**
如果 `DefaultCategories` 不再被直接引用（RecordEntryExpenseCategoryNames 和 LegacyExpenseSubCategoryNames 已删除），从 import 语句中移除 `import com.mewbook.app.domain.model.DefaultCategories`。
  </action>

  <acceptance_criteria>
1. `grep -n "RecordEntryExpenseCategoryNames\|LegacyExpenseSubCategoryNames" app/src/main/java/com/mewbook/app/domain/policy/CategorySelectionPolicy.kt` 无任何输出
2. `grep -n "category.name in\|LegacyExpenseSubCategoryNames\|RecordEntryExpenseCategoryNames" app/src/main/java/com/mewbook/app/domain/policy/CategorySelectionPolicy.kt` 无任何输出
3. `grep -A5 "fun recordSelectionCandidates" app/src/main/java/com/mewbook/app/domain/policy/CategorySelectionPolicy.kt` 显示函数体内只有 `return visibleCategories(categories, type)` 一行
4. 函数签名中 `selectedCategoryId: Long? = null` 参数保留但函数体内不引用
5. AddEditRecordSheet.kt 和 HomeQuickEntryCategoryPolicy.kt 的 `recordSelectionCandidates` 调用点代码保持不变
  </acceptance_criteria>

  <verify>
    <automated>
# 1. 确认无遗留 legacy 引用
grep -n "RecordEntryExpenseCategoryNames\|LegacyExpenseSubCategoryNames" "D:\Codes\AndroidStudioProjects\MewBook\app\src\main\java\com\mewbook\app\domain\policy\CategorySelectionPolicy.kt"

# 2. 确认 recordSelectionCandidates 函数体简化
grep -A5 "fun recordSelectionCandidates" "D:\Codes\AndroidStudioProjects\MewBook\app\src\main\java\com\mewbook\app\domain\policy\CategorySelectionPolicy.kt"

# 3. 确认调用方无需修改
grep -n "recordSelectionCandidates" "D:\Codes\AndroidStudioProjects\MewBook\app\src\main\java\com\mewbook\app\ui\screens\add\AddEditRecordSheet.kt"
grep -n "recordSelectionCandidates" "D:\Codes\AndroidStudioProjects\MewBook\app\src\main\java\com\mewbook\app\domain\policy\HomeQuickEntryCategoryPolicy.kt"

# 4. 全项目编译验证
cd D:/Codes/AndroidStudioProjects/MewBook && ./gradlew :app:compileDebugKotlin 2>&1 | tail -20
    </automated>
  </verify>

  <done>
CategorySelectionPolicy.recordSelectionCandidates 对所有 RecordType 返回统一可见分类列表。没有基于旧二级分类名称的过滤逻辑。调用方 AddEditRecordSheet.kt 和 HomeQuickEntryCategoryPolicy.kt 无需修改即可编译通过。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 无新增 | 本计划为纯内部清理，不引入新信任边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | DefaultCategories.all | accept | 合并后的分类列表仅改变初始化数据，用户可在分类管理中增删，无安全影响 |
</threat_model>

<verification>
# 整体验证

1. 全项目编译通过：`./gradlew :app:compileDebugKotlin`
2. 所有 `legacyExpenseSubCategoryNames`、`flatExpenseAdditions`、`recordEntryExpenseCategories` 引用仅存在于保留文件（BackupImportPolicy.kt、SmartImportPolicy.kt、BackupMigration.kt、测试文件）中
3. `CategorySelectionPolicy.recordSelectionCandidates` 传 EXPENSE 和 INCOME 时行为一致
</verification>

<success_criteria>
1. Category.kt 中不再存在 legacy 字段（grep 验证）
2. CategorySelectionPolicy.kt 不再基于旧名称过滤（grep 验证）
3. 项目全量编译零错误
4. 记账页（AddEditRecordSheet）选择支出分类时，原 87 个分类全部可见
5. 首页快捷入口（HomeQuickEntryCategoryPolicy）显示的分类不再被 legacy 过滤限制
</success_criteria>

<output>
After completion, create `.planning/phases/05-category-cleanup-reorder/05-01-SUMMARY.md`
</output>
