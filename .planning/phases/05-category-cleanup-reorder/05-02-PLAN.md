---
phase: 05-category-cleanup-reorder
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/build.gradle.kts
  - app/src/main/java/com/mewbook/app/data/local/dao/CategoryDao.kt
  - app/src/main/java/com/mewbook/app/domain/repository/CategoryRepository.kt
  - app/src/main/java/com/mewbook/app/data/repository/CategoryRepositoryImpl.kt
  - app/src/main/java/com/mewbook/app/domain/usecase/category/CategoryUseCases.kt
  - app/src/main/java/com/mewbook/app/ui/screens/categories/CategoriesViewModel.kt
  - app/src/main/java/com/mewbook/app/ui/screens/categories/CategoriesScreen.kt
autonomous: true
requirements:
  - CAT-02

must_haves:
  truths:
    - "用户长按分类行可触发拖拽模式，分类行视觉上浮起并可跟随手指移动"
    - "用户将分类拖拽到新位置松手后，分类排在目标位置"
    - "拖拽后的 sortOrder 值持久保存到 Room 数据库，应用重启后顺序不变"
    - "拖拽手柄图标（Icons.Filled.DragHandle）替换原有的上下箭头按钮"
    - "分类列表副标题显示'长按拖拽调整排序'"
    - "所有分类选择界面（记账页、首页快捷入口）显示更新后的排序"
  artifacts:
    - path: "app/build.gradle.kts"
      provides: "reorderable 库依赖"
      contains: "sh.calvin.reorderable:reorderable:2.2.0"
    - path: "app/src/main/java/com/mewbook/app/data/local/dao/CategoryDao.kt"
      provides: "批量更新 sortOrder 的 DAO 方法"
      contains: "updateCategories"
    - path: "app/src/main/java/com/mewbook/app/domain/repository/CategoryRepository.kt"
      provides: "批量更新接口"
      contains: "updateCategories"
    - path: "app/src/main/java/com/mewbook/app/domain/usecase/category/CategoryUseCases.kt"
      provides: "ReorderCategoriesUseCase"
      contains: "class ReorderCategoriesUseCase"
    - path: "app/src/main/java/com/mewbook/app/ui/screens/categories/CategoriesViewModel.kt"
      provides: "moveCategory(fromIndex, toIndex, type)"
      contains: "fun moveCategory"
    - path: "app/src/main/java/com/mewbook/app/ui/screens/categories/CategoriesScreen.kt"
      provides: "拖拽排序 UI"
      contains: "rememberReorderableLazyListState"
      not_contains:
        - "Icons.Filled.KeyboardArrowUp"
        - "Icons.Filled.KeyboardArrowDown"
        - "右侧箭头调整排序"
  key_links:
    - from: "CategoriesScreen LazyColumn"
      to: "rememberReorderableLazyListState.onMove"
      via: "onMove lambda"
      pattern: "rememberReorderableLazyListState"
    - from: "DragHandle Icon Modifier"
      to: "reorderable 库 draggableHandle()"
      via: "Modifier.draggableHandle()"
      pattern: "draggableHandle"
    - from: "moveCategory 方法"
      to: "ReorderCategoriesUseCase"
      via: "use case 调用"
      pattern: "reorderCategoriesUseCase"
---

<objective>
为分类管理页面添加长按拖拽排序功能：引入 sh.calvin.reorderable 库，用 DragHandle 图标替代上下箭头按钮，在 ViewModel 中新增 moveCategory(fromIndex, toIndex, type) 方法并通过批量 sortOrder 重算持久化排序。

Purpose: 提供直观的分类排序交互，用户通过长按拖拽即可调整分类顺序，替换原先逐个点击箭头的繁琐操作。
Output: 可拖拽排序的分类列表 UI、批量 sortOrder 持久化数据层、桥接保留的 moveCategoryUp/moveCategoryDown 键盘备选方法。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-category-cleanup-reorder/05-CONTEXT.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STACK.md

<interfaces>
<!-- Key existing interfaces the executor needs to understand -->

From CategoryDao.kt (current):
```kotlin
@Dao
interface CategoryDao {
    @Query("SELECT * FROM categories WHERE type = :type ORDER BY sortOrder ASC")
    fun getCategoriesByType(type: String): Flow<List<CategoryEntity>>

    @Query("SELECT * FROM categories ORDER BY sortOrder ASC")
    fun getAllCategories(): Flow<List<CategoryEntity>>

    @Update
    suspend fun updateCategory(category: CategoryEntity)
    // ...more
}
```

From CategoryRepository.kt (current):
```kotlin
interface CategoryRepository {
    fun getAllCategories(): Flow<List<Category>>
    suspend fun updateCategory(category: Category)
    suspend fun insertCategories(categories: List<Category>)
    // ...more
}
```

From CategoryUseCases.kt (current — existing classes in this file):
```kotlin
class GetCategoriesUseCase { operator fun invoke(type): Flow<List<Category>>; fun getAll(): Flow<List<Category>> }
class InitializeDefaultCategoriesUseCase { suspend operator fun invoke() }
class AddCategoryUseCase { suspend operator fun invoke(category): Long }
class UpdateCategoryUseCase { suspend operator fun invoke(category) }
class DeleteCategoryUseCase { suspend operator fun invoke(category) }
```

From CategoriesViewModel.kt (current):
```kotlin
@HiltViewModel
class CategoriesViewModel @Inject constructor(
    private val getCategoriesUseCase: GetCategoriesUseCase,
    private val addCategoryUseCase: AddCategoryUseCase,
    private val updateCategoryUseCase: UpdateCategoryUseCase,
    private val deleteCategoryUseCase: DeleteCategoryUseCase
) : ViewModel() {
    fun moveCategoryUp(category: Category)    // swaps sortOrder with previous sibling
    fun moveCategoryDown(category: Category)  // swaps sortOrder with next sibling
    private fun reorderableSiblings(category: Category): List<Category>
}
```

From CategoriesScreen.kt (current — relevant section, lines 184-218):
```kotlin
LazyColumn(...) {
    item { SettingsSummaryCard(..., subtitle = "...右侧箭头调整排序。") }
    item { SettingsSectionHeader(title = "分类列表") }
    items(displayedCategories, key = { it.id }) { category ->
        CategoryRowItem(
            category = category,
            canMoveUp = displayedCategories.firstOrNull()?.id != category.id,
            canMoveDown = displayedCategories.lastOrNull()?.id != category.id,
            onMoveUpClick = { viewModel.moveCategoryUp(category) },
            onMoveDownClick = { viewModel.moveCategoryDown(category) },
            onEditClick = { viewModel.showEditDialog(category) }
        )
    }
}
```

reorderable 库 v2.2.0 预期 API（executor 应在实现时通过 Context7 或库文档确认）:
```kotlin
import sh.calvin.reorderable.ReorderableItem
import sh.calvin.reorderable.rememberReorderableLazyListState

val state = rememberReorderableLazyListState(
    onMove = { from, to -> /* update backing list */ }
)

LazyColumn(
    state = state.listState,
    modifier = Modifier.reorderable(state)
) {
    items(list, key = { it.id }) { item ->
        ReorderableItem(state, key = item.id) { _ ->
            // item content
            Icon(Icons.Filled.DragHandle, modifier = Modifier.draggableHandle())
        }
    }
}
```
</interfaces>

</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add reorderable dependency and batch update data layer</name>
  <files>
    - app/build.gradle.kts
    - app/src/main/java/com/mewbook/app/data/local/dao/CategoryDao.kt
    - app/src/main/java/com/mewbook/app/domain/repository/CategoryRepository.kt
    - app/src/main/java/com/mewbook/app/data/repository/CategoryRepositoryImpl.kt
    - app/src/main/java/com/mewbook/app/domain/usecase/category/CategoryUseCases.kt
  </files>

  <read_first>
Read the following files before making any changes:
1. `app/build.gradle.kts` — understand current dependency block structure (lines 100-150)
2. `app/src/main/java/com/mewbook/app/data/local/dao/CategoryDao.kt` — full file
3. `app/src/main/java/com/mewbook/app/domain/repository/CategoryRepository.kt` — full file
4. `app/src/main/java/com/mewbook/app/data/repository/CategoryRepositoryImpl.kt` — full file
5. `app/src/main/java/com/mewbook/app/domain/usecase/category/CategoryUseCases.kt` — full file
6. Use Context7 or library docs to verify `sh.calvin.reorderable:reorderable:2.2.0` Maven coordinate
  </read_first>

  <action>
**Step 1 — 添加 reorderable 依赖（build.gradle.kts）：**
在 `app/build.gradle.kts` 的 `dependencies { }` 块中，在 Hilt/Room 等核心依赖附近添加：
```kotlin
implementation("sh.calvin.reorderable:reorderable:2.2.0")
```
建议加在 Material Icons Extended 依赖之后（约 line 113 处）。

**Step 2 — CategoryDao 批量更新（CategoryDao.kt）：**
在 `CategoryDao` 接口中添加新的批量更新方法（放在 `updateCategory` 方法附近）：
```kotlin
@Update
suspend fun updateCategories(categories: List<CategoryEntity>)
```
Room 的 `@Update` 注解自动支持 `List` 参数，会在事务中依次更新每个实体。不需要额外的 `@Transaction`。

**Step 3 — CategoryRepository 接口（CategoryRepository.kt）：**
在接口中添加：
```kotlin
suspend fun updateCategories(categories: List<Category>)
```
放在 `updateCategory` 方法下方。

**Step 4 — CategoryRepositoryImpl 实现（CategoryRepositoryImpl.kt）：**
实现新增的接口方法：
```kotlin
override suspend fun updateCategories(categories: List<Category>) {
    categoryDao.updateCategories(categories.map { it.toEntity() })
}
```
复用现有的 `Category.toEntity()` 私有扩展函数。

**Step 5 — ReorderCategoriesUseCase（CategoryUseCases.kt）：**
在文件末尾（`DeleteCategoryUseCase` 之后）添加新的 use case：
```kotlin
class ReorderCategoriesUseCase @Inject constructor(
    private val categoryRepository: CategoryRepository
) {
    suspend operator fun invoke(updates: List<Pair<Long, Int>>) {
        val allCategories = categoryRepository.getAllCategoriesOnce()
        val sortOrderMap = updates.toMap()
        val updatedCategories = allCategories
            .filter { it.id in sortOrderMap }
            .map { it.copy(sortOrder = sortOrderMap[it.id]!!) }
        if (updatedCategories.isNotEmpty()) {
            categoryRepository.updateCategories(updatedCategories)
        }
    }
}
```
此 use case 接收 `List<Pair<Long, Int>>`（categoryId → newSortOrder），从数据库获取最新分类列表，仅更新 sortOrder 字段，保持其他字段不变。
  </action>

  <acceptance_criteria>
1. `grep -n "sh.calvin.reorderable" app/build.gradle.kts` 匹配到 `implementation("sh.calvin.reorderable:reorderable:2.2.0")`
2. `grep -n "suspend fun updateCategories" app/src/main/java/com/mewbook/app/data/local/dao/CategoryDao.kt` 输出 `List<CategoryEntity>` 参数版本
3. `grep -n "suspend fun updateCategories" app/src/main/java/com/mewbook/app/domain/repository/CategoryRepository.kt` 输出 `List<Category>` 参数版本
4. `grep -n "override suspend fun updateCategories" app/src/main/java/com/mewbook/app/data/repository/CategoryRepositoryImpl.kt` 存在实现
5. `grep -n "class ReorderCategoriesUseCase" app/src/main/java/com/mewbook/app/domain/usecase/category/CategoryUseCases.kt` 匹配到类定义
6. Gradle sync 成功：`./gradlew :app:dependencies --configuration debugCompileClasspath 2>&1 | grep reorderable` 找到该依赖
  </acceptance_criteria>

  <verify>
    <automated>
# 1. 验证依赖声明
grep -n "sh.calvin.reorderable" "D:\Codes\AndroidStudioProjects\MewBook\app\build.gradle.kts"

# 2. 验证 DAO 方法
grep -n "updateCategories" "D:\Codes\AndroidStudioProjects\MewBook\app\src\main\java\com\mewbook\app\data\local\dao\CategoryDao.kt"

# 3. 验证 Repository 接口
grep -n "updateCategories" "D:\Codes\AndroidStudioProjects\MewBook\app\src\main\java\com\mewbook\app\domain\repository\CategoryRepository.kt"

# 4. 验证 Repository 实现
grep -A3 "updateCategories" "D:\Codes\AndroidStudioProjects\MewBook\app\src\main\java\com\mewbook\app\data\repository\CategoryRepositoryImpl.kt"

# 5. 验证 UseCase
grep -n "class ReorderCategoriesUseCase" "D:\Codes\AndroidStudioProjects\MewBook\app\src\main\java\com\mewbook\app\domain\usecase\category\CategoryUseCases.kt"

# 6. 全量编译验证
cd D:/Codes/AndroidStudioProjects/MewBook && ./gradlew :app:compileDebugKotlin 2>&1 | tail -20
    </automated>
  </verify>

  <done>
reorderable 库依赖已添加。CategoryDao、CategoryRepository 接口和实现均支持批量更新 categories。ReorderCategoriesUseCase 封装了按 (id, newSortOrder) 映射批量更新分类排序的业务逻辑。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add moveCategory(fromIndex, toIndex, type) and refactor existing move methods</name>
  <files>app/src/main/java/com/mewbook/app/ui/screens/categories/CategoriesViewModel.kt</files>

  <read_first>
Read the following files before making any changes:
1. `app/src/main/java/com/mewbook/app/ui/screens/categories/CategoriesViewModel.kt` — full file
2. `app/src/main/java/com/mewbook/app/domain/usecase/category/CategoryUseCases.kt` — verify ReorderCategoriesUseCase signature
3. `app/src/main/java/com/mewbook/app/domain/policy/CategorySelectionPolicy.kt` — verify visibleCategories signature
  </read_first>

  <action>
**Step 1 — 注入 ReorderCategoriesUseCase：**
在 `CategoriesViewModel` 构造函数中添加：
```kotlin
private val reorderCategoriesUseCase: ReorderCategoriesUseCase
```
添加对应的 import：`import com.mewbook.app.domain.usecase.category.ReorderCategoriesUseCase`

**Step 2 — 新增 moveCategory(fromIndex, toIndex, type) 方法：**
在 `moveCategoryDown` 方法之后添加：
```kotlin
fun moveCategory(fromIndex: Int, toIndex: Int, type: RecordType) {
    if (fromIndex == toIndex) return
    viewModelScope.launch {
        val siblings = CategorySelectionPolicy.visibleCategories(_uiState.value.categories, type)
        if (fromIndex < 0 || fromIndex >= siblings.size || toIndex < 0 || toIndex >= siblings.size) return@launch

        val mutable = siblings.toMutableList()
        val item = mutable.removeAt(fromIndex)
        mutable.add(toIndex, item)

        val updates = mutable.mapIndexed { index, category ->
            category.id to index
        }
        reorderCategoriesUseCase(updates)
    }
}
```
参数 `type: RecordType` 用于确定当前操作的是支出还是收入分类，从而获取正确类型的 siblings 列表。sortOrder 分配策略：使用 `mapIndexed` 为列表中的每个分类分配连续的 `(id, 0)`, `(id, 1)`, `(id, 2)` ... 整数值（per CONTEXT.md decision）。

**Step 3 — 重构 moveCategoryUp/moveCategoryDown 桥接到 moveCategory：**
将现有的 `moveCategoryUp` 和 `moveCategoryDown` 方法体重写为调用 `moveCategory`：

```kotlin
fun moveCategoryUp(category: Category) {
    val siblings = reorderableSiblings(category)
    val currentIndex = siblings.indexOfFirst { it.id == category.id }
    if (currentIndex > 0) {
        moveCategory(fromIndex = currentIndex, toIndex = currentIndex - 1, type = category.type)
    }
}

fun moveCategoryDown(category: Category) {
    val siblings = reorderableSiblings(category)
    val currentIndex = siblings.indexOfFirst { it.id == category.id }
    if (currentIndex < siblings.lastIndex) {
        moveCategory(fromIndex = currentIndex, toIndex = currentIndex + 1, type = category.type)
    }
}
```
保留 `reorderableSiblings` 私有方法，因为桥接方法仍使用它。

**Step 4 — 添加必要的 import：**
确保 `RecordType` 已 import（应该已有，因现有方法使用 `category.type`）。
  </action>

  <acceptance_criteria>
1. `grep -n "fun moveCategory" app/src/main/java/com/mewbook/app/ui/screens/categories/CategoriesViewModel.kt` 匹配到 `fun moveCategory(fromIndex: Int, toIndex: Int, type: RecordType)` 签名
2. `grep -A6 "fun moveCategoryUp" app/src/main/java/com/mewbook/app/ui/screens/categories/CategoriesViewModel.kt` 显示调用 `moveCategory(fromIndex = currentIndex, toIndex = currentIndex - 1, type = category.type)`
3. `grep -A6 "fun moveCategoryDown" app/src/main/java/com/mewbook/app/ui/screens/categories/CategoriesViewModel.kt` 显示调用 `moveCategory(fromIndex = currentIndex, toIndex = currentIndex + 1, type = category.type)`
4. `grep -n "reorderCategoriesUseCase" app/src/main/java/com/mewbook/app/ui/screens/categories/CategoriesViewModel.kt` 匹配到构造函数注入和 moveCategory 内调用两处
5. `grep -n "ReorderCategoriesUseCase" app/src/main/java/com/mewbook/app/ui/screens/categories/CategoriesViewModel.kt` 匹配到构造函数参数声明
6. 编译通过：`./gradlew :app:compileDebugKotlin`
  </acceptance_criteria>

  <verify>
    <automated>
# 验证 ViewModel 方法签名
grep -n "fun moveCategory\|fun moveCategoryUp\|fun moveCategoryDown" "D:\Codes\AndroidStudioProjects\MewBook\app\src\main\java\com\mewbook\app\ui\screens\categories\CategoriesViewModel.kt"

# 编译验证
cd D:/Codes/AndroidStudioProjects/MewBook && ./gradlew :app:compileDebugKotlin 2>&1 | tail -20
    </automated>
  </verify>

  <done>
CategoriesViewModel 新增 moveCategory(fromIndex, toIndex, type) 方法，使用连续整数分配 sortOrder。moveCategoryUp 和 moveCategoryDown 内部桥接到 moveCategory，保留作为键盘/无障碍备选入口。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Replace arrow buttons with drag handle in CategoriesScreen using reorderable library</name>
  <files>app/src/main/java/com/mewbook/app/ui/screens/categories/CategoriesScreen.kt</files>

  <read_first>
Read the following files before making any changes:
1. `app/src/main/java/com/mewbook/app/ui/screens/categories/CategoriesScreen.kt` — full file (598 lines)
2. `app/src/main/java/com/mewbook/app/ui/screens/categories/CategoriesViewModel.kt` — verify moveCategory signature
3. Use Context7 (`npx ctx7@latest library "sh.calvin.reorderable" "reorderable v2.2.0 lazy column API rememberReorderableLazyListState draggableHandle"`) to verify exact API for v2.2.0
  </read_first>

  <action>
**Step 1 — 更新 imports：**
删除不再需要的 import：
- `import androidx.compose.material.icons.filled.KeyboardArrowDown` (line 33)
- `import androidx.compose.material.icons.filled.KeyboardArrowUp` (line 34)

添加新的 import：
```kotlin
import androidx.compose.material.icons.filled.DragHandle
import sh.calvin.reorderable.ReorderableItem
import sh.calvin.reorderable.rememberReorderableLazyListState
```

**Step 2 — 添加 ViewModel 依赖以获得当前 tab 类型：**
在 `CategoriesScreen` composable 函数中（line 102 之后），紧接 `var selectedTabIndex` 声明之后，推导当前类型的变量：
```kotlin
val currentType = if (selectedTabIndex == 0) RecordType.EXPENSE else RecordType.INCOME
```

**Step 3 — 创建 reorderable state：**
在 `CategoriesScreen` composable 中，`LazyColumn` 之前（约 line 184），添加：
```kotlin
val reorderableState = rememberReorderableLazyListState(
    onMove = { from, to ->
        viewModel.moveCategory(
            fromIndex = from.index,
            toIndex = to.index,
            type = currentType
        )
    }
)
```

**Step 4 — 更新 LazyColumn：**
将 LazyColumn 的 `state` 参数换为 `reorderableState.listState`，并添加 `Modifier.reorderable(reorderableState)`：
```kotlin
LazyColumn(
    state = reorderableState.listState,
    modifier = Modifier.reorderable(reorderableState),
    contentPadding = PaddingValues(start = 12.dp, end = 12.dp, top = 16.dp, bottom = 96.dp),
    verticalArrangement = Arrangement.spacedBy(8.dp)
)
```

**Step 5 — 更新副标题文本：**
将 `SettingsSummaryCard` 的 `subtitle`（line 199）从：
```
"当前显示 ${displayedCategories.size} 个分类。点击分类编辑，右侧箭头调整排序。"
```
改为：
```
"当前显示 ${displayedCategories.size} 个分类。点击分类编辑，长按拖拽调整排序。"
```

**Step 6 — 更新 CategoryRowItem 调用：**
将 `items` lambda 中的 `CategoryRowItem` 调用（lines 207-216）替换为：
```kotlin
items(displayedCategories, key = { it.id }) { category ->
    ReorderableItem(reorderableState, key = category.id) { _ ->
        CategoryItemCard(
            category = category,
            onEditClick = { viewModel.showEditDialog(category) }
        )
    }
}
```

**Step 7 — 重写 CategoryRowItem：**
删除原来的 `CategoryRowItem` composable 函数（lines 222-239），不再需要它，因为 `ReorderableItem` 直接包裹 `CategoryItemCard`。

**Step 8 — 重写 CategoryItemCard：**
将 `CategoryItemCard` 签名从：
```kotlin
private fun CategoryItemCard(
    category: Category,
    canMoveUp: Boolean,
    canMoveDown: Boolean,
    onMoveUpClick: () -> Unit,
    onMoveDownClick: () -> Unit,
    onClick: () -> Unit
)
```
改为：
```kotlin
@Composable
private fun CategoryItemCard(
    category: Category,
    onEditClick: () -> Unit
)
```
函数体改为：

```kotlin
Card(
    onClick = onEditClick,
    modifier = Modifier
        .fillMaxWidth()
        .clayCardShadow(),
    shape = RoundedCornerShape(ClayDesign.CardRadius),
    elevation = CardDefaults.cardElevation(defaultElevation = 0.dp),
    colors = CardDefaults.cardColors(
        containerColor = MaterialTheme.colorScheme.surface
    )
) {
    Row(
        modifier = Modifier
            .fillMaxWidth()
            .padding(horizontal = 12.dp, vertical = 8.dp),
        verticalAlignment = Alignment.CenterVertically
    ) {
        // ── 拖拽手柄图标 ──
        Icon(
            imageVector = Icons.Filled.DragHandle,
            contentDescription = "拖拽排序",
            tint = MaterialTheme.colorScheme.onSurfaceVariant,
            modifier = Modifier
                .size(24.dp)
                .draggableHandle()
        )

        Spacer(modifier = Modifier.width(4.dp))

        CategoryIconBadge(
            category = category,
            emphasized = true,
            containerSize = 28.dp,
            iconSize = 15.dp
        )

        Spacer(modifier = Modifier.width(8.dp))

        Column(modifier = Modifier.weight(1f)) {
            Text(
                text = category.name,
                style = MaterialTheme.typography.bodySmall,
                fontWeight = androidx.compose.ui.text.font.FontWeight.SemiBold
            )
            if (category.isDefault) {
                Text(
                    text = "默认分类",
                    style = MaterialTheme.typography.labelSmall,
                    color = MaterialTheme.colorScheme.onSurfaceVariant
                )
            } else {
                Text(
                    text = "自定义",
                    style = MaterialTheme.typography.labelSmall,
                    color = MaterialTheme.colorScheme.onSurfaceVariant
                )
            }
        }
    }
}
```
关键变更：
- 左侧添加 `Icons.Filled.DragHandle` 图标，设置 `Modifier.draggableHandle()` 作为拖拽触发区域
- 名称和标签保留，位于拖拽手柄右侧
- 删除原来的上下箭头按钮列（原 lines 297-321）
- `onClick` 参数重命名为 `onEditClick`
- 移除 `canMoveUp`、`canMoveDown`、`onMoveUpClick`、`onMoveDownClick` 参数

**Step 9 — 确保 DraggableHandle 的 import：**
如果 `draggableHandle()` 需要从 reorderable 库导入，添加：
```kotlin
import sh.calvin.reorderable.draggableHandle
```
（executor 应通过 Context7 或 IDE 自动补全确认准确的 import 路径）
  </action>

  <acceptance_criteria>
1. `grep -c "KeyboardArrowUp\|KeyboardArrowDown" app/src/main/java/com/mewbook/app/ui/screens/categories/CategoriesScreen.kt` 输出 0（箭头图标已完全移除）
2. `grep -n "DragHandle" app/src/main/java/com/mewbook/app/ui/screens/categories/CategoriesScreen.kt` 匹配到 import 和 Icon 使用
3. `grep -n "rememberReorderableLazyListState" app/src/main/java/com/mewbook/app/ui/screens/categories/CategoriesScreen.kt` 匹配到 state 创建
4. `grep -n "ReorderableItem" app/src/main/java/com/mewbook/app/ui/screens/categories/CategoriesScreen.kt` 匹配到 items 包裹
5. `grep -n "draggableHandle" app/src/main/java/com/mewbook/app/ui/screens/categories/CategoriesScreen.kt` 匹配到 DragHandle 图标的 modifier
6. `grep -n "长按拖拽调整排序" app/src/main/java/com/mewbook/app/ui/screens/categories/CategoriesScreen.kt` 匹配到副标题文本
7. `grep -n "右侧箭头调整排序" app/src/main/java/com/mewbook/app/ui/screens/categories/CategoriesScreen.kt` 无任何输出（旧文本已移除）
8. `grep -n "canMoveUp\|canMoveDown\|onMoveUpClick\|onMoveDownClick" app/src/main/java/com/mewbook/app/ui/screens/categories/CategoriesScreen.kt` 无任何输出（旧参数已移除）
9. `grep -n "fun CategoryRowItem" app/src/main/java/com/mewbook/app/ui/screens/categories/CategoriesScreen.kt` 无任何输出（已删除或变为不可见调用）
10. `grep -n "fun CategoryItemCard" app/src/main/java/com/mewbook/app/ui/screens/categories/CategoriesScreen.kt` 仅匹配到新的 2 参数签名 `(category: Category, onEditClick: () -> Unit)`
11. 编译通过：`./gradlew :app:compileDebugKotlin`
  </acceptance_criteria>

  <verify>
    <automated>
# 1. 验证无旧箭头代码残留
grep -c "KeyboardArrowUp\|KeyboardArrowDown" "D:\Codes\AndroidStudioProjects\MewBook\app\src\main\java\com\mewbook\app\ui\screens\categories\CategoriesScreen.kt"

# 2. 验证新 drag handle 代码存在
grep -n "DragHandle\|rememberReorderableLazyListState\|ReorderableItem\|draggableHandle" "D:\Codes\AndroidStudioProjects\MewBook\app\src\main\java\com\mewbook\app\ui\screens\categories\CategoriesScreen.kt"

# 3. 验证副标题更新
grep -n "长按拖拽调整排序\|右侧箭头调整排序" "D:\Codes\AndroidStudioProjects\MewBook\app\src\main\java\com\mewbook\app\ui\screens\categories\CategoriesScreen.kt"

# 4. 验证旧参数已移除
grep -c "canMoveUp\|canMoveDown\|onMoveUpClick\|onMoveDownClick" "D:\Codes\AndroidStudioProjects\MewBook\app\src\main\java\com\mewbook\app\ui\screens\categories\CategoriesScreen.kt"

# 5. 验证 CategoryItemCard 新签名
grep -A1 "fun CategoryItemCard" "D:\Codes\AndroidStudioProjects\MewBook\app\src\main\java\com\mewbook\app\ui\screens\categories\CategoriesScreen.kt"

# 6. 全项目编译
cd D:/Codes/AndroidStudioProjects/MewBook && ./gradlew :app:compileDebugKotlin 2>&1 | tail -20
    </automated>
  </verify>

  <done>
CategoriesScreen 使用 reorderable 库支持长按拖拽排序。DragHandle 图标替代上下箭头按钮。副标题更新为"长按拖拽调整排序"。CategoryItemCard 签名简化，不再接收 move 回调参数。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 用户输入 → sortOrder | 拖拽操作产生的新 sortOrder 值写入数据库，属于受信任的应用内部数据流 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Tampering | moveCategory fromIndex/toIndex | mitigate | 方法内验证 `fromIndex` 和 `toIndex` 在 siblings 列表有效范围内，防止越界访问导致崩溃 |
| T-05-03 | Denial of Service | ReorderCategoriesUseCase | accept | 批量更新 sortOrder 仅涉及同类型分类（最多 87 条），不会造成性能问题 |
</threat_model>

<verification>
# 整体验证

1. 全项目编译：`./gradlew :app:compileDebugKotlin`
2. 运行现有单元测试：`./gradlew :app:testDebugUnitTest`（确保无回归）
3. 手动验证：
   - 打开分类管理页 → 看到 DragHandle 图标和"长按拖拽调整排序"副标题
   - 长按某分类行 → 触发拖拽模式
   - 拖拽到新位置松手 → 分类排在新位置
   - 切换到收入 Tab → 收入分类也可拖拽排序
   - 返回记账页 → 支出分类选择列表显示更新后的排序
   - 重启应用 → 排序保持
</verification>

<success_criteria>
1. 长按分类行可触发拖拽模式，视觉上有浮起效果
2. 拖拽到新位置松手后，分类排在目标位置
3. sortOrder 持久保存，重启后排序不变
4. DragHandle 图标完全替代上下箭头按钮
5. 副标题显示"长按拖拽调整排序"
6. 记账页和首页快捷入口的分类选择列表反映新排序
7. moveCategoryUp/moveCategoryDown 仍可用作编程入口但不暴露在 UI 上
</success_criteria>

<output>
After completion, create `.planning/phases/05-category-cleanup-reorder/05-02-SUMMARY.md`
</output>
